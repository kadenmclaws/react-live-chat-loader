/// <reference types="react" />
interface Props {
    color?: string;
    icon?: 'A' | 'B' | 'C' | 'D';
    iconUrl?: string;
}
declare const Drift: ({ color, icon, iconUrl, }: Props) => JSX.Element | null;
export default Drift;
